const AppError = require('../utils/appError')
const Intensitys = require('../models/Intensity');
const catchAsync = require('../utils/catchAsync')

exports.changeIntensity = catchAsync(async (req, res, next) => {

});